# ref_0001 - Remove Legacy SQL Relation Tables

## Intent

- 그래프 마이그레이션이 완료됨에 따라 더 이상 사용하지 않는 SQL 기반 관계 테이블 및 관련 레거시 쿼리 파일을 제거하여 기술 부채를 청산합니다.

## Plan

- Step 1: `inventory_item`, `player_inventory` 등 관계 전용 테이블 DROP 스크립트 작성
- Step 2: `src/state_db/Query/BASE` 및 `UPDATE` 내의 레거시 SQL 파일 삭제
- Step 3: 스키마 초기화(`schema.py`) 코드에서 해당 테이블 정의 제거

## Safety net

- Tests to rely on: `tests/test_db_logic_full.py`, `tests/test_schema_integrity.py`
- Command:
  - uv run pytest tests

## Done Criteria

- [ ] behavior preserved
- [ ] unit tests passing
- [ ] debt reduced (Legacy SQL code and tables removed)
